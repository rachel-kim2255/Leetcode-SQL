name: Update Progress Log
on:
  push:
    branches:
      - main

jobs:
  update-progress:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0  # 전체 커밋 불러오기

      - name: Count only new SQL files
        id: count
        run: |
          total_sql=$(find . -type f -name "*.sql" | wc -l)
          prev_total=$(grep -Eo '[0-9]+$' README.md | tail -1)
          [ -z "$prev_total" ] && prev_total=0
          added=$((total_sql - prev_total))
          [ $added -lt 0 ] && added=0

          echo "added=$added" >> $GITHUB_ENV
          echo "total_sql=$total_sql" >> $GITHUB_ENV
          echo "today=$(date +'%Y-%m-%d')" >> $GITHUB_ENV

      - name: Update Progress Log
        run: |
          added=$added
          total=$total_sql
          today=$today

          sed -i '/# 📈 Progress Log/,$d' README.md
          echo "" >> README.md
          echo "# 📈 Progress Log" >> README.md
          echo "| Date | Problems Added | Total |" >> README.md
          echo "|------|----------------|--------|" >> README.md

          git log --reverse --pretty=format:"%ad" --date=short --diff-filter=A -- '*.sql' | sort | uniq -c > tmp.log

          total_count=0
          while read count date; do
            total_count=$((total_count + count))
            echo "| $date | 🟢 $count | $total_count |" >> README.md
          done < tmp.log
          rm tmp.log

          echo "" >> README.md
          echo "> **🎯 Total:** $total_count problems solved" >> README.md

      - name: Commit and push
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add README.md
          if ! git diff --cached --quiet; then
            git commit -m "Auto update Progress Log"
            git push
          else
            echo "✅ No new updates."
          fi

      - name: Update main profile README
        run: |
          git clone https://github.com/rachel-kim2255/rachel-kim2255.git profile-repo
          cd profile-repo

          sed -i '/<h3 align="left">📈 SQL-LeetCode Progress Log/,$d' README.md
          echo "" >> README.md
          echo "<h3 align=\"left\">📈 SQL-LeetCode Progress Log</h3>" >> README.md
          echo "" >> README.md
          echo "| Date | Problems Added | Total |" >> README.md
          echo "|------|----------------|--------|" >> README.md
          
          cd ..
          git log --reverse --pretty=format:"%ad" --date=short --diff-filter=A -- '*.sql' | sort | uniq -c > tmp.log

          cd profile-repo
          total_count=0
          while read count date; do
            total_count=$((total_count + count))
            echo "| $date | 🟢 $count | $total_count |" >> README.md
          done < ../tmp.log
          rm ../tmp.log

          echo "" >> README.md
          echo "> **🎯 Total:** $total_count problems solved" >> README.md
          echo "" >> README.md
          echo "🔗 [View full repository → Leetcode-SQL](https://github.com/rachel-kim2255/Leetcode-SQL)" >> README.md

          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add README.md
          git commit -m "Auto sync SQL-LeetCode Progress Log to profile"
          git push


    
     - name: Update main profile README
         run: |
           git clone https://github.com/rachel-kim2255/rachel-kim2255.git profile-repo
           cd profile-repo
    
           # Progress Log 부분 삭제 후 다시 추가
           sed -i '/<h3 align="left">📈 SQL-LeetCode Progress Log/,$d' README.md
           echo "" >> README.md
           echo "<h3 align=\"left\">📈 SQL-LeetCode Progress Log</h3>" >> README.md
           echo "" >> README.md
           echo "| Date | Problems Added | Total |" >> README.md
           echo "|------|----------------|--------|" >> README.md
           
           cd ..
           git log --reverse --pretty=format:"%ad" --date=short --diff-filter=A -- '*.sql' | sort | uniq -c > tmp.log
    
           cd profile-repo
           total_count=0
           while read count date; do
             total_count=$((total_count + count))
             echo "| $date | 🟢 $count | $total_count |" >> README.md
           done < ../tmp.log
           rm ../tmp.log
    
           echo "" >> README.md
           echo "> **🎯 Total:** $total_count problems solved" >> README.md
           echo "" >> README.md
           echo "🔗 [View full repository → Leetcode-SQL](https://github.com/rachel-kim2255/Leetcode-SQL)" >> README.md
    
           git config user.name "github-actions"
           git config user.email "actions@github.com"
           git add README.md
           git commit -m "Auto sync SQL-LeetCode Progress Log to profile"
           git push
    
